# 直接依賴（版本鎖定在目前實際安裝的版本，非 pip freeze 全量輸出）
# 安裝：pip install -r requirements.txt

# 資料處理 / 數值運算
numpy==2.2.1
pandas==2.2.3
scipy==1.15.2
scikit-learn==1.6.1

# 貝氏模型
pymc==5.25.1
arviz==0.22.0

# 資料庫
psycopg2-binary==2.9.12

# 序列化 / 幾何 / 外部資料格式
joblib==1.4.2
shapely==2.1.2
pyreadr==0.5.4

# 繪圖
matplotlib==3.10.0
seaborn==0.13.2

# API
fastapi==0.138.1
uvicorn==0.49.0
pydantic==2.12.5

# 資料擷取
pybaseball==2.2.7
requests==2.32.5

# 測試（httpx2 是 starlette TestClient 的必要相依，沒裝新版 starlette 會直接
# collection error，見 tests/test_api_main.py）
pytest==9.1.1
httpx2==2.11.0
