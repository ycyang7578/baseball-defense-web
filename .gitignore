# 大型資料檔
data/raw/statcast/
data/raw/savant_fielding/
data/raw/positioning/
data/raw/sprint_speed/

# 可由 scripts/precompute_model_oaa.py 重新生成的中間產物（真正的資料在 DB 的 model_oaa 表）
data/precomputed/model_oaa_*.csv

# 內野預算 checkpoint（scripts/precompute_if_optimize.py 可重生成，正式資料在 DB 兩張 precomputed_if_* 表）
data/precomputed/if_positions_rows.csv
data/precomputed/if_gbs_rows.csv

# PyMC 後驗樣本（大型 .nc）
models/**/*.nc

# Python
__pycache__/
*.pyc
*.pyo
.env

# 本機開發工具設定：含 DB 連線字串，不上 repo
.vscode/

# 本機用的目錄樹快照（tree /F /A 產生），非專案內容
project_structure.txt

# Frontend
frontend/node_modules/
frontend/dist/

# 暫存與日誌
logs/
*.log

# pytest
.pytest_cache/
